# Normaliza fim de linha para LF no repositório (evita ruído CRLF no Windows).
* text=auto eol=lf

# Binários — não normalizar.
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.pdf binary
*.woff binary
*.woff2 binary
