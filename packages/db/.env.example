# Conexão com o Postgres do Supabase.
# Migrações (drizzle-kit) usam a conexão direta (porta 5432).
# Runtime serverless deve usar o pooler (porta 6543) com prepare:false.
DATABASE_URL="postgresql://postgres:[SENHA]@db.[PROJECT-REF].supabase.co:5432/postgres"
